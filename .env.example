# Copy to .env and fill in real values. .env is gitignored — never commit it.
# These override the matching keys in config.json at runtime.

XBEE_GATEWAY_MQTT__HOST=mqtt.example.local
XBEE_GATEWAY_MQTT__USERNAME=
XBEE_GATEWAY_MQTT__PASSWORD=
